// El operador ternario en JS es una forma corta y elegante y de una estructura if ...else


// Se llama ternario por que tiene tres operadores, observamos la sintaxis
// Condición ? valorSiverdadero : valorSiFalso

// Ejemplo 1

let age = 18;
let message = age >= 18 ? "Eres mayor de edad" : "Eres menor de edad";

console.log(message);

// Ejemplo 2
